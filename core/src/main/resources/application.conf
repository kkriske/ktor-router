ktor {
    deployment {
        port = 8080
        environment = develop
        autoreload = true
        watch = [ ktor-router ]
    }

    application {
        id = app
        modules = [ com.oshprojects.example.core.ApplicationKt.main ]
    }
}
