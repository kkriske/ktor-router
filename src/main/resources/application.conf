ktor {
    deployment {
        port = 8080
        environment = develop
        autoreload = true
        watch = [ app ]
    }

    application {
        id = app
        modules = [ app.ApplicationKt.main ]
    }
}